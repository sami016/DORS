image: microsoft/dotnet:latest

stages:
  - build
  - test

variables:
  project: "DORS"

before_script:
  - "dotnet restore"

build:
  stage: build
  variables:
    build_path: "$project"
  script:
    - "ls"
    - "dotnet build"

test:
  stage: test
  variables:
    test_path: "$(project).Test.Integration"
  script:
    - "cd $test_path"
    - "dotnet test"